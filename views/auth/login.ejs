<!--  views/auth/login.ejs -->

<div class="header">
  <h1 id="main-title" class="container"><%= __('Login to your Birds Account') %></h1>
  <!-- <h3 class="container">You're looking at: <code><%= view.pathFromApp + '.' +view.ext %></code></h3> -->
</div>
<div class="container">
  <% if (req.session.flash && req.session.flash.error) { %><%=req.flash('error')%><% } %>
  <form action="/login" method="post">
    <div class="form-group">
      <label for="inputEmail">Email</label>
      <input type="email" name="username" id="inputEmail" class="form-control" placeHolder="Email">
    </div>
    <div class="form-group">
      <label for="inputPassword">Password</label>
      <input type="password" name="password" id="inputPassword" class="form-control" placeHolder="Password">
    </div>
    <input type="submit" value="Submit" class="btn btn-primary">
  </form>
</div>
